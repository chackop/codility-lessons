// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');

function solution(A) {
  // let expectSum = 0;
  // if (A.length == 0 || A[0] != 1) {
  //   return 1;
  // } else if (A[A.length - 1] != A.length + 1) {
  //   return A.length + 1;
  // }
  // for (let index = 0; index < A.length; index++) {
  //   if (index + 1 < A.length && A[index + 1] - A[i] > 1) {
  //     expectSum = A[index] + 1;
  //     break;
  //   }
  // }
  // return expectSum;
  // // Loop solution
  // let expectSum = 0;
  // let calcSum = 0;
  // for (let index = 0; index < A.length + 1; index++) {
  //   expectSum += index;
  //   if (index < A.length) {
  //     calcSum += A[index];
  //   }
  // }
  // return expectSum - calcSum;
}
